# AI 伴读 · 手测执行卡（2026-07-09 · 第二轮）

> 本卡对应**第二轮手测**：覆盖上一轮你反馈的全部问题点修复后的复测。
> 用法：浏览器开 `http://127.0.0.1:8123` → 访问 `/enter` 自动登录 → 按下方步骤操作，在 `[ ]` 打勾或记录差异。
> 服务已确认在线（PID 22408）。本轮已把你的 GLM 配置修正为正确的 OpenAI 兼容端点 `/api/paas/v4`，真实 AI 现已可用。

---

## 0. 前置准备
- [ ] 浏览器开 `http://127.0.0.1:8123/enter`，自动登录进书架
- [ ] 书架里有 EPUB 书（若无，导入 `ai-reading-companion/tests/fixtures/sample-qixue.epub`）
- [ ] AI 设置已配 GLM（base_url=`https://open.bigmodel.cn/api/paas/v4`，model=`glm-4-flash`，已为你修正）

---

## 1. AI 面板流式回复（修复：协议不匹配致气泡空白）
**根因**：你的 base_url 原指向 `/api/anthropic`（Anthropic 端点）但协议选了 openai → 代码按 OpenAI 形状解析 Anthropic 帧 → 200 但 0 token → 气泡空白。
- [ ] 打开一本书，在 AI 面板输入框直接输入「用一句话解释什么是气血」并发送
- [ ] **预期**：AI 气泡逐字流式出现真实回答（如「气血是中医理论中…」），不再空白
- [ ] 选中书中一句话 → 点「💬 问 AI」→ 预期：面板弹出并流式解读，有真实内容
- 实际：__________________

## 2. AI 协议不匹配时的兜底（修复：不再静默失败）
- [ ] 去「AI 设置」把 base_url 故意改回 `https://open.bigmodel.cn/api/anthropic`（协议保持 openai），保存
- [ ] 回阅读页问 AI → **预期**：先流出离线演示回复（有字），末尾附说明「模型未返回有效内容，当前协议是 OpenAI 但 base_url 指向 Anthropic 端点…请改用 /api/paas/v4 或改协议为 Anthropic」
- [ ] 测试连接按钮 → **预期**：提示「连接返回但无有效内容，…协议不匹配」（不再误报成功）
- [ ] **测完请把 base_url 改回 `https://open.bigmodel.cn/api/paas/v4` 保存**
- 实际：__________________

## 3. 「原文」不再重复（修复：选中即问时原文显示两次）
- [ ] 选中一句 → 点「💬 问 AI」→ **预期**：用户气泡里显示选中的文字，**不再**在下方重复一块「📖 原文：…」（因为 content 与 context 相同时不显示引用块）
- [ ] 手动在输入框输入问题（不选中文字）→ 预期：正常，无引用块
- 实际：__________________

## 4. 划线（修复：getSelectionCfi is not a function）
**根因**：epub.js 0.3.93 无 `rendition.getSelectionCfi()`，划线静默失败。
- [ ] 选中书中一句 → 点「🖍 划线」→ **预期**：黄色高亮出现 + toast「🖍 已划线」
- [ ] 刷新页面 → **预期**：高亮仍在（CFI 落库）
- [ ] 点已有高亮 → 弹菜单 → 点「🗑 删除划线」→ **预期**：高亮消失
- [ ] 三个主题（浅色/护眼/深色）下各试一次划线 → **预期**：均成功
- 实际：__________________

## 5. 出测验 · 选中文字（修复：读不到 iframe 选区）
**根因**：`generateQuiz` 用 `window.getSelection()`，但文字在 EPUB iframe 内，父窗口选区为空 → 总提示「请先选中」。
- [ ] 先在书中选中一段文字（选中后会弹出工具条）
- [ ] 点「📝 出测验」→ 选「选中文字」→ 点「生成测验」
- [ ] **预期**：据选中文字出题（不再提示「请先选中」）
- [ ] 选答案 → 提交 → **预期**：判分 + 逐题对错 + 解析 + 得分
- [ ] 留一题不答提交 → **预期**：提示「还有 N 题没选」
- [ ] 点「导出」→ **预期**：下载 md（含 `[[双链]]`+frontmatter）
- 实际：__________________

## 6. PDF 内联查看（修复：原仅占位+新标签）
- [ ] 导入 `sample-qixue.pdf` 或打开已有 PDF 书
- [ ] **预期**：页面内直接用浏览器原生 PDF 查看器显示（可滚动/缩放），不再是占位卡片
- [ ] 右上角「↗ 新标签打开」仍可用
- 实际：__________________

## 7. RAG vault 路径持久化（修复：填了地址直接重建索引时新路径没入库）
- [ ] 进「🧠 记忆」页，把 vault 路径改成 `D:/03_DevData/Projects/ai-reading-companion/tests/fixtures/obsidian-vault`
- [ ] 直接点「🔄 重建索引」（不先点保存）→ **预期**：自动保存当前路径 + 索引成功，提示「已保存路径并重建索引」
- [ ] 刷新页面 → **预期**：vault 路径保持新填的值（不再回旧地址）
- [ ] 故意填一个不存在的路径保存 → **预期**：提示「⚠️ vault 路径不存在：…」
- 实际：__________________

## 8. 脑图深度（修复：真实 AI + 两层结构）
- [ ] 进「📊 脑图」→ 点「生成/重新生成」
- [ ] **预期**：逐章真实总结（非离线泛泛而谈），脑图有「章节 → 核心要点 → 细节子项」两层以上结构
- [ ] 生成耗时约数十秒（真实模型逐章调用），状态有提示
- 实际：__________________

## 9. 闪卡复习页桌面布局（优化：居中限宽 + 键盘快捷键）
- [ ] 存几张闪卡后进「闪卡」页
- [ ] **预期**：概览三卡（待复习/剩余/总闪卡），复习卡居中限宽（不再拉满全宽）
- [ ] 键盘按「空格」→ **预期**：卡片翻面
- [ ] 键盘按「1」→ **预期**：标记不认识、下一张；按「2」→ 认识、下一张
- 实际：__________________

## 10. AI 面板模式（魔鬼/苏格拉底）—— 现已可用（依赖 AI 修复）
- [ ] 开「🎯 魔鬼代言人」→ 问一句 → **预期**：真实挑刺回答（非离线演示）
- [ ] 关魔鬼、开「🧭 苏格拉底」→ 问一句 → **预期**：只问不答的真实引导
- [ ] 两个都关 → **预期**：普通伴读回答
- 实际：__________________

## 11. 术语解释（📖 解释）—— 现已可用
- [ ] 选中一个术语（如「气血」）→ 点「📖 解释」→ **预期**：气泡显示真实模型结合语境的解释（非离线占位）
- 实际：__________________

## 12. 四大图谱 —— 现走真实 AI
- [ ] 概念图谱 / 人物图 / 论证图 → 点「生成」→ **预期**：真实抽取节点（非离线模式标签）
- [ ] 脑图详见第 8 项
- 实际：__________________

---

## 复测结论
- 全部通过 ☐　有差异（见上方「实际」记录）☐
- 发现的新问题请直接贴控制台报错 + 操作步骤，我当场修。

---
*本卡由本轮修复自动生成。修复清单：①AI 流式空响应/传输异常兜底 ②划线 CFI 版本鲁棒 ③测验读 iframe 选区 ④PDF 内联查看 ⑤RAG 路径持久化+校验 ⑥脑图两层结构 ⑦闪卡桌面布局+键盘 ⑧原文去重 ⑨协议不匹配检测。*
